# Devpost Submission Pack

## Project name
Morsegrid Outfitters — AI Revenue Recovery Agent

## Elevator pitch (one line)
A multi-agent system on Gemini 3 + Google ADK that recovers lost ecommerce revenue from
both abandoned carts and dormant customers, reasoning over live data through the MongoDB
MCP server.

---

## Inspiration
Stores leak revenue from two leaks at once: ~70% of carts are abandoned, and a long tail of
past customers quietly goes dormant. The standard fix — a fixed Klaviyo/Postscript drip that
fires the same template at everyone — ignores *why* each person left. We wanted an agent that
actually reasons over each shopper's history and the live catalog and writes something a real
store employee would write.

## What it does
Three cooperating Gemini 3 agents work one ranked queue that mixes two opportunity types:
- **Abandoned carts** — reassure about the exact item, suggest in-stock alternatives if it
  sold out, or "complete the look" for multi-item carts.
- **Dormant customers** — warm win-backs tied to a real reason (a new arrival matching their
  history, or the item they once searched for is finally in stock).

A **Planner** ranks every opportunity by expected recovered margin; a **Nurturer** (Gemini 3
+ ADK + MongoDB MCP + Atlas Vector Search) drafts a personalized message; a human approves it
in a Streamlit dashboard; a **Sender** picks the best channel (email/SMS/IG), delivers, and
logs the result. It even switches an email-dead shopper to SMS automatically.

## How we built it
- **Gemini 3 Flash** on **Vertex AI** for all agent reasoning and drafting.
- **Google ADK** (Agent Builder, code-first) for the multi-agent orchestration and tooling.
- **MongoDB MCP Server** as the agents' *only* runtime database interface (`find`,
  `aggregate`, `count`, `insert-many`).
- **MongoDB Atlas Vector Search** over Vertex `text-embedding-004` for product matching.
- A **Shopify-style webhook** (`webhook_server.py`) normalizes `checkouts/abandoned` payloads
  into a storefront-agnostic schema, so any ecommerce platform can plug in.
- **Streamlit** for the human-in-the-loop dashboard; **Resend** for real email; deployed on
  **Google Cloud Run**.

## Technologies used
Gemini 3 Flash, Google ADK, Vertex AI, Google Cloud Run, MongoDB Atlas, MongoDB MCP Server,
Atlas Vector Search, text-embedding-004, Streamlit, Resend, Shopify webhook format.

## Data sources
100% synthetic demo data for a fictional motorcycle-gear store: 50 products, 18 customers,
~180 behavior events, 6 abandoned carts. Generated by `data/seed_data.py`; embedded and
indexed by `data/build_index.py`. Real Shopify/WooCommerce stores plug in via the webhook.

## Challenges
- Doing vector search *through* an agent without making the LLM hand-type 768-float vectors —
  solved with an `embed_query` + `$vectorSearch` tool that the agent calls by intent.
- Keeping two very different opportunity types (carts decay in hours; dormancy in weeks)
  comparable in a single ranked queue — solved by scoring both in expected-margin dollars.
- Running the Node-based MongoDB MCP server alongside Python in one Cloud Run container.

## What we learned
Routing all agent DB access through MCP made the agent dramatically simpler to reason about
and audit — every tool call is visible in the trace. The expected-value Planner is what turns
"send everyone an email" into a defensible business decision.

## What's next
Real Twilio SMS + Meta IG, a holdout group for measured incremental-revenue lift, and an
autonomous mode that sends low-risk recoveries without human review.

---

# Demo video script (target 2:50, hard cap 3:00)

> Record at 1080p. Turn ON English captions/subtitles (required). No third-party logos.
> Pre-step before recording: run the Planner once so the queue is warm, then reset drafts.

**[0:00–0:18] Hook + problem**
*On screen:* title slide → dashboard empty state.
*VO:* "Online stores lose revenue two ways: nearly 70% of carts are abandoned, and past
customers quietly go dormant. Morsegrid is an AI agent that recovers both — built on Gemini 3,
Google ADK, and MongoDB."

**[0:18–0:40] The unified queue**
*On screen:* click **Run Planner**; the queue fills.
*VO:* "The Planner scans MongoDB and ranks every opportunity — abandoned carts and dormant
customers together — by expected recovered dollars. Right now there's $2,000+ of cart value at
risk." Point at the **Cart $ at Risk** metric.

**[0:40–1:25] Scenario 1 — high-value cart (the MCP money shot)**
*On screen:* open **Mike** ($599 carbon helmet, payment step). Click **Personalize Message**.
Expand **Agent Reasoning Trace**.
*VO:* "Mike left a $599 helmet at the payment step. The Nurturer pulls his session from
MongoDB — every blue call is the **MongoDB MCP server** — then runs Atlas Vector Search."
Read the drafted email referencing the exact item. Click **Approve & Send**; cut to the inbox
showing the delivered email.

**[1:25–1:55] Scenario 2 — sold-out → alternatives**
*On screen:* open **Diego**; Personalize. Show recommended product IDs.
*VO:* "Diego's item sold out after he left. Instead of a dead link, vector search surfaces
in-stock alternatives — Kevlar jeans at $190 — and the agent writes the apology + suggestions."

**[1:55–2:20] Scenario 3 — automatic channel switch**
*On screen:* open **Sarah**; Personalize; Approve. Highlight the green banner
"Delivered via SMS — email cold, SMS opt-in active."
*VO:* "Sarah hasn't opened her last five emails, so the Sender automatically switches her to
SMS. The agent picks the channel — not a fixed rule."

**[2:20–2:40] Dormant re-engagement + ingestion**
*On screen:* open **Ava** (or Marcus); show the win-back draft. Then click **Inject test
abandoned cart**.
*VO:* "Same pipeline re-engages dormant VIPs with new arrivals matching their history. And new
carts arrive through a Shopify-style webhook — so any store can plug in."

**[2:40–2:55] Close**
*On screen:* the trace + queue.
*VO:* "Gemini 3 plans, writes, and decides; MongoDB MCP is the only door to the data; a human
approves every send. Live on Cloud Run, open-source under MIT."
